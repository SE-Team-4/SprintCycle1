After cloning, run the following in your command line inside the project folder:
1. npm init
2. npm i mongodb
3. npm i ejs
4. npm i express --save
Then:
Create a new file with the name: .env
Inside .env, write DB_STRING = <database string here>
We will put the DB string on discord.